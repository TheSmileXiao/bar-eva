import React from 'react';
import { Nav, NavLink, NavIcon, Bars } from './NavbarElements';

const Navbar = () => {
  return (
    <Nav>
      <NavLink to='/'>Café-Bar Elena</NavLink>
    </Nav>
  );
};

export default Navbar;
